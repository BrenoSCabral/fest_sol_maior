<%= form_with(model: short) do |form| %>
  <% if short.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(short.errors.count, "error") %> prohibited this short from being saved:</h2>

      <ul>
        <% short.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
